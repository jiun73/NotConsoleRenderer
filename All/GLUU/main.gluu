
new style GLUU key



row px 500 <
	col <
		row button CollisionX			{ $collisionx_main }
		row button TicTacToe			{ $tictactoe_main }
		row button { "Connect 4" }		{ $connect4_main } 
		row button BounceX				{ $bouncex_main } 
		row button { "USA Simulator" }	{ $usasim_main } 
	>

	col <
		row button Chatroom				{ $chatroom_main } 
		row button AnimMaker			{ $animmaker_main } 
		row button Pong					{ $pong_main }  
		row button VlanHelper			{ $vlan_helper }  
		row button { "Netpong" }		{ $main_fight }  
	>
>